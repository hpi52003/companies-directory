• Set up a new React project using Vite for fast development.

• Installed and configured Tailwind CSS for smooth, clean UI styling.

• Created a responsive layout to display company details using cards.

• Added filter options (search, dropdowns) so users can easily find companies.

• Used a local JSON file as a mock API to load company data into the UI.

• Implemented pagination to browse companies page by page.

• Added sorting options to organize companies by name or industry.

• Used React Hooks for managing state and handling filter logic.

• Ensured proper loading and error states for a better user experience.

• Stored company data and filter logic in reusable components.

• Tested UI responsiveness on different screen sizes.

• Uploaded the entire project to GitHub for version control.

• Deployed the app online using a hosting platform (Vercel / Netlify).

• Prepared a short video explaining the application and code structure.
